language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t kamiour/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kamiour/docker-react npm run test
